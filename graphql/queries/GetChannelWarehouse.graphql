query GetChannelWarehouse($channel_id: ID!) {
  channel(id: $channel_id) {
    warehouses {
      address {
        city
        country {
          code
          country
        }
        countryArea
        phone
        postalCode
        streetAddress1
        streetAddress2
      }
    }
  }
}
